const BASE_DOMAIN = 'https://dinesykmeldte.labs.nais.io';

// Editable configuration
export default {
    ITERATIONS: 5,
    CONCURRENT_REQUESTS: 5,
    BASE_DOMAIN,
    URL: `${BASE_DOMAIN}/arbeidsgiver/sykmeldte`,
};
